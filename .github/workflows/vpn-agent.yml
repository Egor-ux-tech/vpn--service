name: vpn-agent

on:
  push:
    branches: [main]
    paths: ["vpn/**", ".github/workflows/vpn-agent.yml"]
  pull_request:
    paths: ["vpn/**", ".github/workflows/vpn-agent.yml"]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vpn
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: vpn/pyproject.toml

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Ruff check
        run: ruff check .

      - name: Ruff format check
        run: ruff format --check .

      - name: mypy
        run: mypy app

      - name: pytest
        run: python -m pytest -q

      - name: pip-audit (dependency vulnerability scan)
        run: pip install pip-audit && pip-audit --strict --skip-editable

  docker:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Build vpn-agent image
        run: docker build -f infrastructure/docker/vpn-agent.Dockerfile -t vpn-service-agent:ci ./vpn
